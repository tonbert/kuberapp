apiVersion: apps/v1
kind: Deployment
metadata:
  name: server-puppetmaster
spec:
  replicas: 3
  selector:
    matchLabels:
      component: server
  template:
    metadata:
      labels:
        component: server
    spec:
      containers:
        - name: server
          image: hammeran/kuberapp-server
          ports:
            - containerPort: 5000